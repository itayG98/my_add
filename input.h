#ifndef INPUT_H
#define INPUT_H

/* Prompt the user to insert number in (base 2)*/
unsigned int getBaseTwoNum();
/* Validate if number is in base two (zero / one)*/
int isBaseTwo(unsigned int toCheck);

#endif