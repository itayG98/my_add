#ifndef CALCULATOR_H
#define CALCULATOR_H

// Adds two unsigned integers that represent binary numbers (base 2)
unsigned int my_add(unsigned int a, unsigned int b);
unsigned int decimal_to_binary(unsigned int a, unsigned int b);

#endif // CALCULATOR_H